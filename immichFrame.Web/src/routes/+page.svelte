<script lang="ts">
	// The default slideshow is disabled — slideshows are served via named /[slug] links.
	// +page.ts redirects to /admin, so this is shown only momentarily (if at all) during navigation.
</script>

<svelte:head>
	<title>ImmichFrame</title>
</svelte:head>

<div class="flex min-h-screen items-center justify-center bg-black text-slate-500">Redirecting…</div>
